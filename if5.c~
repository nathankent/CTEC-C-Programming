#include <stdio.h>

int main()

{
	int a,b,c;
	
	printf("Please input three int's to organise in ascending order : \n");
	scanf(" %i, %i , %i", &a,&b,&c);
	
	if (a>=b, b<=c && c<=a)
		printf("%i\n%i\n%i\n", a,b,c);
	
	return 0;

}
